{
    "name": "flask-react-monorepo",
    "workspaces": ["frontend"],
    "private": true,
    "scripts": {
      "dev": "npm run dev --workspace=frontend",
      "start": "flask run --prefix backend",
      "install": "npm install --workspaces"
    }
  }
  